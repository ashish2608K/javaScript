const checkEligibility = function (name, age, runningTime) {
    return (name, age, runningTime)

    if (age <= 20 && runningTime === "15 mins") {
        console.log("You are Eligible")
    }

    else {
        console.log("You are not Eligible");

    }
};


checkEligibility("ashish",20,13);